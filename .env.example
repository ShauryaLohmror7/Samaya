# Google Gemini API key for the end-of-day reflection feature.
# Free — generate one at https://aistudio.google.com (Google AI Studio).
# The app works fully without it; only the AI reflection is disabled.
GEMINI_API_KEY=
